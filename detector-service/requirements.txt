# Base service deps, shared by both detection backends.
# Backend-specific deps live in requirements-<backend>.txt so the image only
# carries the one it runs (rfdetr and transformers each pull a large torch tree).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pillow>=10.0.0
